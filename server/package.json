{
  "name": "civil-bim-collaboration-server",
  "version": "1.0.0",
  "description": "Optional collaboration backend for Civil BIM Viewer — annotation sync, GitHub OAuth, shareable viewpoints",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "npx tsx index.ts",
    "dev": "npx tsx watch index.ts"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "tsx": "^4.19.0"
  }
}
